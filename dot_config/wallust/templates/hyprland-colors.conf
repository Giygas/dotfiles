# Generated by wallust

{% for i in range(16) %}
$color{{ i }} = rgba({{ colors[i] | strip }}ff)
{% endfor %}
$background = rgba({{ background | strip }}ff)
$foreground = rgba({{ foreground | strip }}ff)
